<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.thingsboard</groupId>
	<artifactId>springdoc-openapi</artifactId>
	<version>2.8.8TB2</version>
	<packaging>pom</packaging>
	<name>Spring openapi documentation</name>
	<description>Spring openapi documentation</description>
	<url>https://github.com/thingsboard/springdoc-openapi</url>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.8</version>
	</parent>

	<licenses>
		<license>
			<name>The Apache License, Version 2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
		</license>
	</licenses>

	<organization>
		<name>ThingsBoard</name>
		<url>https://thingsboard.io/</url>
	</organization>

	<developers>
		<developer>
			<name>Badr NASS LAHSEN</name>
			<email>support@springdoc.org</email>
			<organization>springdoc</organization>
			<organizationUrl>https://springdoc.org/</organizationUrl>
		</developer>
		<developer>
			<id>ikulikov</id>
			<name>Igor Kulikov</name>
			<email>ikulikov@thingsboard.io</email>
			<organization>ThingsBoard</organization>
			<organizationUrl>https://thingsboard.io/</organizationUrl>
		</developer>
	</developers>

	<scm>
		<url>https://github.com/thingsboard/springdoc-openapi</url>
		<connection>scm:git:https://github.com/thingsboard/springdoc-openapi.git</connection>
		<developerConnection>scm:git:git@github.com:thingsboard/springdoc-openapi.git</developerConnection>
		<tag>v2.8.8TB2</tag>
	</scm>

	<modules>
		<module>springdoc-openapi-starter-common</module>
		<module>springdoc-openapi-starter-webmvc-api</module>
		<module>springdoc-openapi-starter-webflux-api</module>
		<module>springdoc-openapi-starter-webmvc-ui</module>
		<module>springdoc-openapi-starter-webflux-ui</module>
		<module>springdoc-openapi-bom</module>
		<module>springdoc-swagger-ui</module>
	</modules>

	<properties>
		<build-helper-maven-plugin.version>3.6.0</build-helper-maven-plugin.version>
		<maven-gpg-plugin.version>1.6</maven-gpg-plugin.version>
		<maven-release-plugin.version>2.5.3</maven-release-plugin.version>
		<central-publishing-maven-plugin.version>0.7.0
		</central-publishing-maven-plugin.version>
		<flatten-maven-plugin.version>1.5.0</flatten-maven-plugin.version>
		<swagger-api.version>2.2.30</swagger-api.version>
		<swagger-ui.version>5.21.0TB2</swagger-ui.version>
		<gmavenplus-plugin.version>1.13.1</gmavenplus-plugin.version>
		<jjwt.version>0.9.1</jjwt.version>
		<therapi-runtime-javadoc.version>0.15.0</therapi-runtime-javadoc.version>
		<spring-cloud-function.version>4.2.2</spring-cloud-function.version>
		<spring-security-oauth2-authorization-server.version>1.4.3</spring-security-oauth2-authorization-server.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- swagger dependencies -->
			<dependency>
				<groupId>io.swagger.core.v3</groupId>
				<artifactId>swagger-core-jakarta</artifactId>
				<version>${swagger-api.version}</version>
			</dependency>
			<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt</artifactId>
				<version>${jjwt.version}</version>
			</dependency>
			<!-- spring Cloud Function-->
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-function-dependencies</artifactId>
				<version>${spring-cloud-function.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!-- spring Security Oauth2 Authorization Server-->
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-oauth2-authorization-server</artifactId>
				<version>${spring-security-oauth2-authorization-server.version}</version>
			</dependency>
			<!-- swagger ui -->
			<dependency>
				<groupId>org.thingsboard</groupId>
				<artifactId>springdoc-swagger-ui</artifactId>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>${build-helper-maven-plugin.version}</version>
				<executions>
					<execution>
						<id>add-licence-resource</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>add-resource</goal>
						</goals>
						<configuration>
							<!--
							  Appended as an extra resource (rather than a <resources> declaration,
							  which would replace the filtered resources inherited from
							  spring-boot-starter-parent) so that maven-source-plugin picks the
							  licence files up into the sources jars.
							-->
							<resources>
								<resource>
									<directory>${basedir}/..</directory>
									<targetPath>META-INF</targetPath>
									<includes>
										<include>LICENSE</include>
										<include>COPYRIGHT</include>
									</includes>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-resources</id>
						<phase>validate</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${basedir}/target/classes/META-INF</outputDirectory>
							<resources>
								<resource>
									<directory>${basedir}/..</directory>
									<includes>
										<include>LICENSE</include>
										<include>COPYRIGHT</include>
									</includes>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>ci</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<modules>
				<module>springdoc-openapi-starter-common</module>
				<module>springdoc-openapi-starter-webmvc-api</module>
				<module>springdoc-openapi-starter-webflux-api</module>
				<module>springdoc-openapi-starter-webmvc-ui</module>
				<module>springdoc-openapi-starter-webflux-ui</module>
				<module>springdoc-openapi-tests</module>
			</modules>
		</profile>
		<profile>
			<id>gpg</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-source-plugin</artifactId>
						<executions>
							<execution>
								<id>attach-sources</id>
								<goals>
									<goal>jar</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-javadoc-plugin</artifactId>
						<executions>
							<execution>
								<id>attach-javadocs</id>
								<goals>
									<goal>jar</goal>
								</goals>
							</execution>
						</executions>
						<configuration>
							<sourcepath>src/main/java</sourcepath>
							<source>17</source>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-resources-plugin</artifactId>
						<executions>
							<execution>
								<id>copy-licence-into-javadoc</id>
								<phase>prepare-package</phase>
								<goals>
									<goal>copy-resources</goal>
								</goals>
								<configuration>
									<!--
									  Runs before javadoc:jar (package), so the licence files end up
									  inside the javadoc jars too. The path below is where the
									  maven-javadoc-plugin version managed by spring-boot-starter-parent
									  (3.10.1) writes its output.
									-->
									<outputDirectory>${project.build.directory}/reports/apidocs/META-INF</outputDirectory>
									<resources>
										<resource>
											<directory>${basedir}/..</directory>
											<includes>
												<include>LICENSE</include>
												<include>COPYRIGHT</include>
											</includes>
										</resource>
									</resources>
								</configuration>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>${maven-gpg-plugin.version}</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<artifactId>maven-release-plugin</artifactId>
						<version>${maven-release-plugin.version}</version>
						<configuration>
							<tagNameFormat>v@{project.version}</tagNameFormat>
							<autoVersionSubmodules>true</autoVersionSubmodules>
							<useReleaseProfile>false</useReleaseProfile>
							<releaseProfiles>gpg</releaseProfiles>
							<goals>deploy</goals>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
	<distributionManagement>
		<repository>
			<id>thingsboard-public-repo</id>
			<name>ThingsBoard Public Repo</name>
			<url>https://repo.thingsboard.io/artifactory/libs-release-public</url>
		</repository>
	</distributionManagement>
	<repositories>
		<repository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</pluginRepository>
	</pluginRepositories>
</project>
